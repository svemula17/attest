[project]
name = "attest"
version = "0.1.0"
description = "Continuous compliance control plane with a governed agent layer (PoC)"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "sqlalchemy>=2.0",
  "alembic>=1.13",
  "apscheduler>=3.10,<4",
  "httpx>=0.27",
  "python-multipart>=0.0.9",
  "itsdangerous>=2.2",
  "pydantic>=2.7",
]

[project.optional-dependencies]
dev = ["pytest>=8"]
llm = ["anthropic>=1.0"]

[project.scripts]
attest = "attest.cli:main"
attest-mcp = "attest.mcp_server:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-data]
attest = ["migrations/alembic.ini", "migrations/script.py.mako", "migrations/versions/*.py"]

[tool.setuptools.packages.find]
include = ["attest*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-p no:langsmith_plugin"
